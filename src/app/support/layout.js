export const metadata = {
  title: "Support",
  description:
    "Get support for Nutrichef - Indian Food Tracker & Calorie Counter.",
};

export default function SupportLayout({ children }) {
  return <>{children}</>;
}
